# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database"

# Prisma direct URL (Neon direct endpoint for migrations/introspection)
# Use the Neon "direct" connection string (no pgbouncer query param).
DIRECT_URL="postgresql://USER:PASSWORD@EP-XXXX.us-east-2.aws.neon.tech/neondb?sslmode=require&connect_timeout=15"

# Optional: Direct connection for Prisma Studio
# DIRECT_URL="postgresql://user:password@ep-example-123456.us-east-1.aws.neon.tech/neondb?sslmode=require"